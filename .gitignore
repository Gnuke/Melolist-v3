# === 레거시 참조용 (커밋 대상 아님) ===
nuxt-app/

# === 환경변수 / 시크릿 ===
.env
.env.*
!.env.example
*.pem
*.key

# === Node / 프론트엔드 (frontend: Vite + React) ===
node_modules/
dist/
dist-ssr/
.vite/
*.local
coverage/
.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Java / 백엔드 (backend: Spring Boot + Gradle) ===
build/
.gradle/
out/
bin/
*.class
*.jar
!**/gradle/wrapper/gradle-wrapper.jar

# === IDE ===
.idea/
*.iml
*.ipr
*.iws
.vscode/
!.vscode/extensions.json

# === OS ===
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# === 로그 / 임시 파일 ===
*.log
logs/
*.tmp
*.swp

# === Claude Code 로컬 설정 ===
.claude/settings.local.json
# 스킬 심볼릭 링크는 로컬 생성물 (본체는 .agents/skills + skills-lock.json 커밋)
.claude/skills/

# === 매칭률 측정(§10 C6) — 오디오·리포트·실측 manifest는 로컬 전용 (원칙 IV) ===
backend/scripts/match-rate/audio/
backend/scripts/match-rate/reports/
backend/scripts/match-rate/manifest.json
