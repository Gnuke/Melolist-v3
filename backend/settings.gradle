plugins {
	// 로컬에 JDK 21이 없는 환경(팀원/CI 대체)에서는 Gradle이 toolchain(JDK 21)을 자동 프로비저닝한다.
	// 로컬에 JDK 21이 있으면 그것을 자동 감지해 사용한다.
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'backend'
