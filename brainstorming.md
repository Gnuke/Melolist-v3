# Melolist-v3 OKR 브레인스토밍 — 2026 Q3 (7~9월)

> 작성일: 2026-07-08 · 팀: 1인 개발(Gnuke) · 현재 위치: M1(기반) 마무리 단계, M2(검색) 착수 직전
> **✅ 결정(2026-07-08): 안 A(검색 품질 중심) 채택** — "품질 먼저" 방향. 상세는 0장 참조.

---

## 0. 확정 OKR — 2026 Q3

> **Objective**: 처음 방문한 게스트가 30초 안에 흥얼거린 곡을 찾아내는, 신뢰할 수 있는 검색 경험을 완성한다.

| # | Key Result | 목표치 | 측정 방법 |
|---|---|---|---|
| KR1 | 검색 매칭률 (내부 테스트 곡 셋) | 지문 ≥ 80% · 허밍 ≥ 50% | 테스트 곡 셋(지문 30곡 + 허밍 20곡)을 측정 스크립트로 `/api/search/*`에 반복 투입, Top-3 내 정답 포함률 |
| KR2 | 검색 응답 시간 | p95 ≤ 6초 (녹음 종료→결과 표시) | 서버: 요청 수신→응답 완료 타이밍 로그. 클라: 종료 버튼→렌더 완료 계측(개발 빌드) |
| KR3 | 첫 방문 내 검색 완료율 (베타) | ≥ 70% | 이벤트 로그: `visit` 대비 `search_result_shown` 세션 비율. **베타 코호트 10~20명 전제** |

### KR별 실행 메모

**KR1 — 매칭률**
- 테스트 곡 셋 구성 기준: 장르 분산(K-pop/팝/OST/힙합/클래식), 발매연도 분산, 허밍은 남/녀·원키/이조 섞기
- 정답 판정: ACRCloud 응답의 acrid/제목-아티스트가 기대값과 일치하면 성공. Top-1이 아닌 **Top-3 기준**(결과 리스트 UX 전제)
- ⚠️ **M2 첫 주에 허밍 스파이크 선행**: ACRCloud 허밍 API 성능이 50% 목표에 근본적으로 못 미치면 목표치를 재조정해야 함 — 2주 안에 판정
- 측정 스크립트는 `backend`에 두고 CI와 무관하게 수동 실행 (분기 중 3회 이상: M2 중간·M2 완료·분기 말)

**KR2 — 응답 시간**
- 병목 예상 지점: 오디오 업로드 크기(webm 인코딩), ACRCloud 왕복, MUSIC upsert. 구간별 타이밍 로그로 분해 측정
- p95 산출 모수: 테스트 곡 셋 실행 로그 + 베타 실사용 로그 합산

**KR3 — 검색 완료율**
- 필요 이벤트(최소): `visit`(세션 시작) · `search_started`(녹음 시작) · `search_result_shown`(결과 표시) · `search_failed`(무결과/오류) — **M2 개발 범위에 포함**
- 이벤트는 서버 로깅 우선(PRD 결정: 측정 도구는 M6, 지금은 원천만 확보)
- 베타 모집이 분기 내 불발되면 KR3은 미측정 처리하고 KR1·KR2로 분기 평가 (KR3 목표를 낮추지 않음)

### 헬스 메트릭 (관찰만, 목표 없음)
- 검색→저장 전환율·리뷰 작성률·재방문율(D7/D30): 해당 기능(M3/M4)이 생기는 시점부터 이벤트 원천만 수집
- 빌드/배포 상태: CI 통과율 (안 C는 미채택이지만 파이프라인 방치 방지용)

### 주간 체크인 (Radical Focus)
- 매주 금요일: KR별 현재값 + 자신감 점수(1~10) 기록 → 이 문서 하단에 append

---

## 1. 전략 컨텍스트 (PRD 기반)

| 구분 | 내용 |
|---|---|
| 비즈니스 목표 | 인식 → **저장** → **공유**로 이어지는 리텐션 루프. "검색 도구" → "개인 음악 아카이브 + 커뮤니티"로 포지션 확장 |
| 제품 목표 | 비회원도 검색 즉시 사용, 저장/공유에서 로그인 유도. 모바일 한 손 UX. AI 확장 가능한 구조 |
| 확정 KPI | 검색 매칭률 · 검색→저장 전환율 · 리뷰 작성률 · 재방문율(D7/D30) |
| 마일스톤 | M2(검색) → M3(저장) → M4(커뮤니티)가 Q3 사정권. M5(AI)·M6(하드닝)은 Q4 유력 |

**KPI ↔ OKR 관계**: 위 4개 KPI는 장기 추적 지표이고, 아래 각 안의 KR은 이 KPI 자체(또는 그 선행 지표)의 **이번 분기 기대 변화량**을 표현한 것이다. KR로 채택되지 않은 KPI는 헬스 메트릭으로 병행 관찰한다.

**공통 전제(데이터 가용성)** ⚠️
- 아직 사용자가 없다. 사용자 지표 KR은 **비공개 베타 코호트(지인 10~20명) 모집**을 전제로 하며, 이것이 안 되면 해당 KR은 측정 불능이다.
- KPI 측정 도구는 M6 예정이므로, Q3에는 **서버 이벤트 로깅(검색 시도/성공, 저장, 가입, 방문)** 이 선행되어야 모든 KR이 측정 가능하다. 어떤 안을 고르든 이 로깅은 필수 기반 작업.
- 매칭률처럼 사용자 없이도 잴 수 있는 지표는 **내부 테스트 곡 셋**으로 측정한다(재현 가능·객관적).

---

## 2. OKR 후보 3안

### 안 A — 핵심 경험 완성형: "검색이 진짜 된다" ✅ 채택

> **Objective**: 처음 방문한 게스트가 30초 안에 흥얼거린 곡을 찾아내는, 신뢰할 수 있는 검색 경험을 완성한다.

**Key Results**
1. 내부 테스트 곡 셋(지문 30곡 + 허밍 20곡) 기준 **검색 매칭률 ≥ 80% (지문) / ≥ 50% (허밍)**
2. 녹음 종료 → 결과 표시 **p95 응답 시간 ≤ 6초**
3. 베타 사용자의 **첫 방문 내 검색 완료율 ≥ 70%** (방문 대비 결과 화면 도달, 이벤트 로그 기준)

**근거**: 검색은 서비스의 존재 이유이자 리텐션 루프의 입구다. 입구가 부실하면 저장·커뮤니티는 의미가 없다. M2에 집중 투자하고 품질 기준선을 수치로 박아두는 안. 매칭률 KR은 ACRCloud 성능 + 녹음 품질(전처리) + upsert 파이프라인을 통합 검증한다.

**리스크**: 저장(M3)이 분기 내 미착수될 수 있음 → 리텐션 루프 검증이 Q4로 밀림.

---

### 안 B — 리텐션 루프 검증형: "찾은 곡이 라이브러리에 쌓인다"

> **Objective**: 검색으로 끝나던 v2를 넘어, 찾은 곡이 개인 라이브러리에 쌓이고 다시 돌아오게 만드는 루프의 첫 회전을 증명한다.

**Key Results**
1. 베타 코호트 **검색→저장 전환율 ≥ 25%** (검색 성공 세션 대비 즐겨찾기/플레이리스트 담기)
2. **게스트→가입 전환율 ≥ 30%** (저장 시도 시 로그인 유도 화면 → 가입 완료)
3. 베타 코호트 **D7 재방문율 ≥ 20%**

**근거**: PRD 비즈니스 목표(리텐션 루프)를 그대로 분기 목표로 삼는 안. M2+M3를 한 분기에 관통해야 하므로 가장 야심적이다. "검색 도구 → 아카이브" 포지션 전환이 실제로 사용자 행동으로 나타나는지를 최초로 검증한다.

**리스크**: 3개 KR 전부 베타 모집 성공에 의존. 개발 범위(M2+M3+이벤트 로깅)도 가장 넓어 1인 개발 부하 최대. 확신도 60% 아래로 내려가면 KR 목표치를 낮추기보다 안 A로 후퇴 권장.

---

### 안 C — 출시 준비 완성형: "언제든 배포할 수 있는 상태"

> **Objective**: 기능 개발 속도를 잃지 않으면서, 누구에게 보여줘도 부끄럽지 않은 '항상 배포 가능한' 서비스 기반을 만든다.

**Key Results**
1. **main 병합 → 운영 반영 리드타임 ≤ 15분** (경로별 CI/CD 파이프라인 완전 자동화, 수동 개입 0회)
2. 핵심 플로우 4종(검색·가입·저장·리뷰) **E2E 자동 테스트 통과율 100%** 를 매 배포마다 유지
3. 확정 KPI 4종의 **원천 이벤트가 100% 수집·조회 가능** (누락률 < 1%, SQL로 주간 리포트 산출 가능)

**근거**: PRD 기술 목표(CI/CD·컨테이너 배포)와 "측정 원천 확보" 결정을 분기 목표로 격상한 안. Q4(M5 AI, M6 하드닝)를 빠르게 달리기 위한 활주로를 먼저 깐다. 1인 개발에서 자동화는 곧 개발 속도다.

**리스크**: 분기 말에도 사용자 가치(검색·저장)가 눈에 보이지 않을 수 있음 → 동기 저하 위험. KR3은 산출물 성격이 있어 outcome 지표로는 가장 약함.

---

## 3. 비교 & 논의 포인트

| | 안 A (검색 품질) | 안 B (리텐션 루프) | 안 C (출시 기반) |
|---|---|---|---|
| 정렬된 PRD 목표 | 제품 목표 | 비즈니스 목표 | 기술 목표 |
| 커버 마일스톤 | M2 | M2+M3 | 인프라 + 로깅 |
| 베타 모집 의존도 | KR 1개 | KR 3개 전부 | 없음 |
| 1인 개발 부하 | 중 | **상** | 중 |
| 야심도(확신도 60~70% 기준) | 적정 | 상한선 | 적정 |

**논의할 질문**
1. Q3 안에 베타 사용자를 모을 수 있는가? → 가능하면 B, 불확실하면 A가 현실적
2. 배포 파이프라인 없이 베타를 돌릴 수 있는가? → 어렵다면 C의 KR1을 A/B에 헬스 메트릭으로 편입하는 절충안도 가능
3. 허밍 매칭률 50%가 ACRCloud 성능상 현실적인지 → M2 초기에 스파이크로 먼저 검증 권장

**절충안 스케치(참고)**: A의 O + KR(매칭률, 첫 검색 완료율) + C의 KR3(이벤트 수집) — 검색 품질과 측정 기반을 같이 가져가는 보수적 조합.

---

## 4. 다음 단계

- [x] 세 안 중 방향 선택 → **안 A 채택 (2026-07-08)**, 확정 내용은 0장
- [x] 선택한 KR의 측정 방법 구체화 → 0장 "KR별 실행 메모"
- [x] 미채택 KPI는 헬스 메트릭으로 등록 → 0장 "헬스 메트릭"
- [ ] **허밍 스파이크** (M2 첫 2주 내): ACRCloud 허밍 성능 확인 → KR1 허밍 50% 목표 유지/조정 판정. **7.4의 메타/이미지 확인 항목 겸행**(identify 응답 실측·커버리지·ACRCloud 약관 확인)
- [ ] 테스트 곡 셋 구성(지문 30 + 허밍 20) — 곡 리스트 + 녹음 샘플 준비
- [ ] 매칭률 측정 스크립트 작성 (Top-3 판정, backend 수동 실행)
- [ ] 이벤트 4종(`visit`/`search_started`/`search_result_shown`/`search_failed`) 서버 로깅 — M2 개발 범위에 포함
- [ ] 구간별 타이밍 로그(업로드/ACRCloud/upsert) — KR2 분해 측정용
- [ ] 베타 코호트 10~20명 모집 계획 (KR3 전제, M2 완료 시점 목표)
- [ ] 주간 체크인 시작: 매주 금요일 KR 현재값 + 자신감 점수(1~10)를 이 문서 하단에 기록

## 5. 주간 체크인 로그

| 날짜 | KR1 지문/허밍 | KR2 p95 | KR3 완료율 | 자신감(1~10) | 메모 |
|---|---|---|---|---|---|
| 2026-07-08 | 미측정 | 미측정 | 미측정 | 7 | OKR 확정. M2 착수 전 |

---

## 6. M2 검색 설계 논의 (2026-07-08)

> 근거: 현재 프론트 구현(`features/search/`), PRD §9.1 검색 Flow, 확정 OKR(0장).
> 각 항목은 **제안 + 근거 + 열린 결정**으로 구성. ✅=제안대로 확정, ❓=사용자 결정 대기.

### 6.1 검색 UX (가장 중요)

**현재 플로우**: 탭 선택 → 마이크 → 녹음 모달(수동 정지) → 재생 카드 확인 → [검색] 버튼 수동 클릭 → 결과.
클릭이 3회 이상 필요하고, "녹음 확인" 단계가 O(30초 안에 곡 찾기)와 충돌한다.

**제안: 모드별 플로우 분리** ❓
| | 지문 (음악 찾기) | 허밍 (직접 부르기) |
|---|---|---|
| 녹음 종료 | **12초 자동 종료** (카운트다운 링) + 조기 정지 허용 | 수동 정지 (최대 20초 컷) |
| 검색 시점 | **정지 즉시 자동 검색** (Shazam 패턴) | 재생 확인 후 수동 [검색] (현행 유지) |
| 근거 | 주변 음악은 재생해볼 이유가 없음 — 확인 단계는 순수 마찰 | 내 목소리는 확인·재녹음 욕구가 실재 (원키로 다시 부르기 등) |

- 30초 예산 분해(지문): 권한 ~2s + 녹음 12s + 인식 ≤6s(KR2) + 결과 스캔 ~5s = **~25s** → O 달성 가능
- 재생 카드는 자동 검색 후에도 유지 → 실패 시 "같은 녹음으로 재검색"·"다시 녹음" 선택지 확보
- 녹음 최소 길이: 지문 3초 / 허밍 8초 미만이면 업로드 전 경고 (6.3 사전 검증과 연동)
- 업로드 포맷: webm/opus 현행 유지, KR2 구간별 타이밍 로그 확인 후 다운샘플링 여부 결정

### 6.2 검색 결과 화면

현재: 제목 + (지문: 아티스트·앨범·연도 / 허밍: 일치율%) + [듣기](유튜브 새 탭). 스켈레톤 로딩 있음.

**M2에서 더할 것 (제안)**
1. ✅ **Top-3 고정 노출** — KR1 판정 기준(Top-3)과 정합. 지문은 보통 1건, 허밍은 최대 3건
2. **결과 카드 이미지** ✅(방침 변경) — 텍스트만으로는 "이 곡 맞네" 확신이 약함. 실측 메타 샘플(`references/ACRCloud-meta.txt`) 분석 결과 **유튜브 썸네일이 아니라 앨범 커버가 1순위**로 재정의됨. 소싱 전략·폴백 체인·스파이크 항목은 **7장** 참조
3. **즐겨찾기 버튼 미리 노출** ❓ — PRD Flow에 "즐겨찾기(로그인 유도)"가 명시됨. M2에선 저장 기능 없이 **버튼 + "로그인하면 저장됩니다" 유도 시트**까지만. 작업량이 작고, 게스트→가입 전환 이벤트 원천을 미리 쌓는 효과. (저장 동작 자체는 M3)
4. ✅ 듣기 = **새 탭 유지** — 인라인 임베드/미니플레이어는 M3(저장·라이브러리와 함께)
5. ✅ 지문 결과에 score 계속 숨김(사실상 확정 매칭), 허밍만 일치율 노출 (현행)

### 6.3 검색 실패 UX

실패를 4유형으로 분해하고 각각 다르게 처리 (현재는 2·3번이 한 덩어리):

| # | 유형 | 감지 | 화면 (제안) |
|---|---|---|---|
| F1 | **녹음 품질 불량** (무음·너무 짧음) | **업로드 전 클라 검증**: 길이 체크 + RMS(음량) 체크 | "소리가 거의 녹음되지 않았어요. 마이크를 가까이 하고 다시 시도해주세요" + [다시 녹음]. **서버 왕복 없음** |
| F2 | **무결과** (ACRCloud no match) | 서버 응답 빈 배열 | 모드별 카피 + 전환 제안: 지문 실패 → "직접 불러보시겠어요?" (허밍 탭 유도) / 허밍 실패 → 팁 노출("후렴구를 10초 이상") + [다시 녹음] |
| F3 | **저신뢰** (허밍 score<50) | 현행 로직 유지 | 최고 1건 + "일치율이 낮지만…" (현행) + [다시 불러보기] CTA 추가 |
| F4 | **오류** (네트워크/서버/ACRCloud 장애) | HTTP 오류/타임아웃 | "일시적인 문제가 발생했어요" + **[재시도] — 같은 blob 재전송(재녹음 불필요)**. 서버 raw 에러 메시지 노출 제거(현재 `err.message` 노출 중) |

- F1이 최우선: 베타에서 실패의 상당수는 무음/짧은 녹음일 것 → 서버 비용 0으로 예방, KR1 분모 오염도 방지
- F4의 "blob 보존 + 재시도"가 두 번째: 네트워크 오류로 녹음을 날리는 게 최악의 경험
- 모든 실패는 `search_failed(reason: bad_audio|no_match|low_score|error)`로 기록 → 실패 분포가 다음 개선 우선순위를 정함

### 6.4 이벤트 로그 설계

**저장소**: Supabase Postgres 테이블 (파일 로그 ✗ — SQL 주간 리포트가 OKR 측정 방법이므로)

```sql
create table event_log (
  id          bigint generated always as identity primary key,
  event_type  text not null,          -- 'visit' | 'search_started' | 'search_result_shown' | 'search_failed'
  session_id  uuid not null,          -- 게스트 추적용 (프론트 생성)
  user_id     uuid,                   -- 로그인 시에만
  properties  jsonb not null default '{}',
  created_at  timestamptz not null default now()
);
create index on event_log (event_type, created_at);
```

**세션 식별**: 프론트가 UUID 생성 → `sessionStorage` 보관(탭 세션 = KR3의 "방문" 단위) → `X-Session-Id` 헤더로 전송. IP 저장 안 함, 게스트 PII 없음.

**수집 경로**: `POST /api/events` (인증 불요, fire-and-forget — 실패해도 UX 영향 0). 단 `search_*` 이벤트 중 서버가 직접 아는 것(검색 요청·결과 수·타이밍)은 **서버가 검색 처리 중에 직접 기록** — 클라 이벤트는 visit·렌더 완료 등 클라만 아는 것으로 한정.

**이벤트 사전 (M2 4종)**
| event | 발생 시점 | properties |
|---|---|---|
| `visit` | 앱 로드(세션당 1회) | `referrer`, `is_mobile` |
| `search_started` | 녹음 시작 | `mode` |
| `search_result_shown` | 결과 렌더 완료 | `mode`, `result_count`, `top_score`, `client_ms`(녹음종료→렌더) |
| `search_failed` | 실패 확정 | `mode`, `reason`(bad_audio/no_match/low_score/error), `http_status` |

**서버 타이밍 로그(KR2)**: 검색 요청당 `search_request` 레코드 — `total_ms`, `acr_ms`, `meta_ms`, `upsert_ms`, `audio_bytes`, `mode`, `matched`(bool). event_log와 같은 테이블에 properties로 넣거나 별도 테이블 — **같은 테이블 + event_type='search_request'로 시작(단순), 쿼리 불편해지면 분리**.

**SearchHistory와의 관계**: SearchHistory는 로그인 사용자의 도메인 데이터(내 기록 화면용, M3에 UI), event_log는 익명 계측. **역할이 달라 분리 유지**.

**KR 산출 쿼리 (검증용)**
- KR3 = `search_result_shown` 세션 수 ÷ `visit` 세션 수 (기간 내, distinct session_id)
- KR2 = `search_request.total_ms`의 p95

### 6.5 MVP 범위 확정 (M2)

**In Scope**
| 영역 | 항목 |
|---|---|
| 백엔드 | `POST /api/search/fingerprint`·`/humming` (ACRCloud 서명·multipart), 유튜브 메타 보강(videoId·썸네일), **MUSIC upsert(acrid 기준, eager 캐시)**, SearchHistory 기록(**로그인 사용자만**, 조회 UI 없음), `POST /api/events` + 서버 타이밍 로그 |
| 프론트 | 자동 검색 플로우(6.1 확정 시), 실패 UX 4유형(6.3), 결과 Top-3·썸네일(6.2 확정 시), 즐겨찾기 유도 버튼(6.2-3 확정 시), 이벤트 발화 |
| 측정 | 테스트 곡 셋(지문30+허밍20) + 매칭률 측정 스크립트, **허밍 스파이크(첫 2주)** |

**Out of Scope (명시적 제외)**
- 검색 기록 조회 화면 (`GET /search/history` UI) → M3 (기록 write만 M2)
- 즐겨찾기/플레이리스트 실제 저장 → M3
- 인라인 재생·미니플레이어 → M3
- 텍스트/자연어 검색 → M5
- Bottom Navigation 등 잔여 UX 개선 → M3 이후
- 오디오 원본 저장 → 안 함(부록 A 확정)

### 6.6 Definition of Done — M2 완료 조건

아래 전부 충족 시 M2 완료 선언 + `v0.2.0` 태그:

1. **E2E**: 로컬 실기동(백+프론트+Supabase)에서 지문·허밍 각각 녹음→결과 표시까지 성공. 모바일 브라우저(실기기) 1회 이상 데모
2. **품질 게이트**: 테스트 곡 셋 매칭률 1차 측정 완료 — 지문 ≥80% 충족, 허밍은 스파이크 판정 반영한 목표 기준. 미달 시 M2 연장이 아니라 **원인 분석 티켓 + 목표 재협상**으로 처리
3. **성능**: p95 측정치 산출 가능(타이밍 로그 동작). ≤6초 미달이면 병목 구간 식별까지는 M2 내 완료
4. **실패 UX**: F1~F4 각각 정의된 화면·카피로 동작 (raw 에러 노출 0건)
5. **이벤트**: 4종+`search_request`가 실제 기록되고, KR2·KR3 산출 SQL이 값을 반환
6. **테스트**: search 도메인 단위+통합 테스트(ACRCloud는 mock) CI 통과
7. **문서**: API 계약(요청/응답 스키마) 문서화, PRD·brainstorming.md 결정사항 반영
8. **git**: `feat/m2-*` 브랜치 전부 main 병합, 워킹트리 깨끗

### 6.7 결정 대기 목록 ❓

| # | 결정 | 제안(추천) |
|---|---|---|
| D1 | 지문 모드 자동 검색 + 12초 자동 종료 | 채택 (Shazam 패턴, O와 직결) |
| D2 | 허밍 모드는 확인 후 수동 검색 유지 | 채택 (재녹음 욕구 실재) |
| D3 | 결과 카드에 이미지 추가 | 채택 — 단 **유튜브 썸네일이 아니라 앨범 커버가 1순위** (ACRCloud Metadata API의 `album.covers`), 유튜브 videoId 썸네일은 폴백. 상세는 7장 |
| D4 | 즐겨찾기 버튼 M2 노출(로그인 유도만) | 채택 (M3 연결고리 + 전환 원천) |

---

## 7. 결과 이미지 소싱 전략 — ACRCloud 메타 실측 분석 (2026-07-08)

> 근거: `references/ACRCloud-meta.txt` — ACRCloud **Metadata API** 실측 응답(Blank Space 예시).
> 6.2-2와 D3의 "유튜브 썸네일" 논의를 이 장으로 대체·구체화.
> **개정(같은 날, 저작권·복잡도 검토 반영)**: 폴백 체인 5단→3단 간소화 · preview 사용 계획 철회(저장도 안 함) · 이미지 재호스팅 금지 원칙 추가.

### 7.1 샘플에서 확인된 사실

| # | 필드 | 내용 | 판정 |
|---|---|---|---|
| 1 | `album.cover` + `album.covers.{small\|medium\|large}` | **앨범 커버 직접 제공** (64/300/3000px, Apple Music CDN=mzstatic). **최상위 필드** — ACRCloud가 이미 정규화한 값 | ✅ 1순위 이미지 소스 |
| 2 | `external_metadata.youtube[].id` | videoId (예: `dC9QIUKviJU`). 링크는 music.youtube.com이지만 videoId는 일반 YouTube와 공유 | ✅ 썸네일 폴백 + 듣기 링크(v2 방식 유지) |
| 3 | `external_metadata.applemusic[].album.cover` / `deezer[].album.cover` | 추가 커버 소스 2종 | ❌ 사용 안 함 — 파싱·검증 복잡도 대비 이득 없음 (7.3) |
| 4 | `external_metadata.applemusic[].preview` | 30초 AAC 미리듣기 URL | ❌ 사용 안 함 — 저작권 위험 최상위 등급 (7.2) |
| 5 | ⚠️ `spotify` 항목에 applemusic 데이터가 그대로 들어있음 | ACRCloud 쪽 데이터 품질 이슈로 추정 | external_metadata에서 `youtube[].id`만 읽는 설계라 영향 없음. youtube link 도메인 검증만 유지 |

### 7.2 저작권 위험 등급 — 쓸 것과 안 쓸 것

| 위험 | 항목 | 판정 | 근거 |
|---|---|---|---|
| 없음 | 제목·아티스트·앨범명·발매일 (텍스트) | 사용 | 사실 정보는 저작물이 아님. ACRCloud 인식 서비스의 본체(v2도 사용) |
| 낮음 | 앨범 커버 **핫링크** (mzstatic) | 사용 | 유튜브 썸네일(i.ytimg.com, D3 기채택)과 **동일 위험 등급** — 저작권 이미지를 권리자 본인의 공개 CDN에서 핫링크. URL 문자열만 저장, 이미지는 브라우저가 직접 로드. Metadata API 자체가 인식 결과 표시용으로 파는 유료 상품이고, 커버 표시는 인식 앱(Shazam류) 업계 관행 |
| 낮음 | 유튜브 videoId 썸네일 (i.ytimg.com) | 사용 (폴백) | 위와 동일 등급 |
| **높음** | Apple Music 30초 preview (음원) | **사용 안 함** | 이미지가 아닌 **음원 서빙** — 질적으로 다른 단계. Apple 자사 스토어 홍보 맥락용 + URL 만료 가능성. **M2에서 preview_url 저장도 하지 않음.** M3 인라인 재생 논의 시 ACRCloud/Apple 약관 확인 후 재론 |

**금지 원칙 — 이미지 프록시·재호스팅 금지**: 이미지를 우리 서버로 다운로드·프록시·캐싱하지 않는다. 핫링크(URL 저장)와 달리 복제·전송에 해당해 위험 등급이 실제로 올라감. `cover_url` 문자열만 저장하고 이미지 바이트는 만지지 않는다.

**v2 설계 계승**: 듣기 = videoId 파싱 → `https://www.youtube.com/watch?v={id}` 새 탭 (유튜브 뮤직 유료계정 회피 + 재생·수익은 권리자 플랫폼에서). 이미지는 이 옆에 붙는 확신 UX일 뿐, 재생 구조는 불변.

### 7.3 폴백 체인 — 3단 확정 (간소화)

1. `album.covers.medium` (300px, 최상위 필드 — **플랫폼 배열 파싱 없음**)
2. videoId → `https://i.ytimg.com/vi/{id}/mqdefault.jpg` (**mqdefault**=320×180, 16:9 검은 띠 없음. hqdefault는 4:3 띠 생김)
3. 음표 아이콘 플레이스홀더 (클라)

- `external_metadata`에서는 `youtube[].id` 하나만 읽음(v2와 동일 범위) → applemusic/deezer 배열 파싱과 7.1-5 같은 오염 검증 문제가 통째로 소멸. 서버 추출 로직은 몇 줄 수준
- 역할 분담: 서버가 1·2 평가해 `cover_url` 1개 확정(+`youtube_video_id` 별도 전달), 클라는 `<img onError>` 시 videoId 썸네일 → 플레이스홀더 2단 폴백만

### 7.4 스파이크 확인 항목 (허밍 스파이크에 합쳐서 진행)

- [ ] **identify(`/v1/identify`) 응답의 `external_metadata` 실측**: 이 샘플은 Metadata API 응답. identify가 자체적으로 `youtube.vid`·커버를 주는지 확인 — vid만 주고 커버가 없다면 Metadata API 호출은 커버 확보를 위해서라도 유지(6.2-2의 "왕복 생략 가능" 판단 뒤집힘)
- [ ] **`album.covers` 커버리지 실측**: 테스트 곡 셋 30곡 기준 빈 비율 — K-pop/OST에서 높으면 유튜브 썸네일 폴백이 주력이 되는지 판단
- [ ] **허밍 결과의 메타 풍부도**: 허밍 acrid로 Metadata API 조회가 되는지, 커버가 나오는지
- [ ] **ACRCloud 서비스 약관의 metadata 사용 범위 1회 정독** (표시용은 문제없을 가능성이 높지만 확인)
- [ ] 핫링크 실확인 1회: mzstatic · i.ytimg.com `<img>` 로드 (리퍼러 차단 여부)

### 7.5 KR2(p95 ≤ 6초)와의 관계

- Metadata API 왕복(`meta_ms`)은 **유튜브 링크 + 커버** 두 가지 가치를 가지므로 유지가 기본값
- 비용 통제 3가지:
  1. **eager 캐시**: MUSIC upsert에 cover_url·youtube_video_id 저장 → 같은 acrid 재검색 시 meta_ms=0
  2. 허밍 Top-3는 meta 조회 3건 **병렬** 실행
  3. v2 규칙 유지: score≤0.5 결과는 보강 생략 → 플레이스홀더
- upsert를 응답 반환과 분리(응답 후 비동기) 검토 → `total_ms`에서 `upsert_ms` 제거 가능. 구간별 타이밍 로그(6.4) 확인 후 결정

### 7.6 M2 스키마/구현 반영 (6.5 In Scope 보정)

| 영역 | 반영 내용 |
|---|---|
| DB (MUSIC upsert) | 컬럼: `acrid`(key)·`title`·`artist`·`album`·`release_date`·**`cover_url`**·**`youtube_video_id`**. preview_url은 저장하지 않음(7.2) |
| API 응답 (`AcrResult`) | `cover_url?`·`youtube_video_id?` 추가 (`youtube_url` 유지 — 듣기 버튼용) |
| 프론트 | 결과 카드 좌측 56px 라운드 정사각 이미지 + 스켈레톤에 이미지 자리 추가(`SearchResultsList.tsx`), onError 2단 폴백 |
| 서버 | 커버 추출: `album.covers.medium ?? ytimg(videoId) ?? null` 수준의 단순 로직. youtube link 도메인 검증. **이미지 프록시/재호스팅 금지(7.2)** |
