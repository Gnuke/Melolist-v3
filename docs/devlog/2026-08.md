# Development Log — 2026-08

## 2026-08-05 — KR1 공식 런: 지문 29/30(96.7%) · 허밍 14/20(70%) — 양측 통과 (코드 무변경 — manifest·오디오는 로컬 전용)

허밍 12곡을 추가 녹음해 곡 셋(지문 30 + 허밍 20)이 완성됐고, 첫 **공식 KR1 런**을 돌렸다(07-29는 지문만 풀셋, 허밍은 8곡 파일럿). 분기 3회 측정의 공식 1회차.

**실행**: 07-29와 동일 — 로컬 백엔드+실 ACR 키(운영 KR2/KR3 이벤트 오염 방지), PS로 `.env` 파싱 env 주입 → `gradlew bootRun` → health UP → `node match-rate.mjs`(50곡, 간격 2s, 약 4분). 리포트 `reports/match-rate-2026-08-05T03-29-54.json`(커밋 금지). 신규 12곡은 manifest 사전 등록(한글 제목+로마자 아티스트) + 기존 검증 acrid 5곡 선기입. 측정 후 백엔드 종료.

| 모드 | raw | 검증 보정 후 | KR1 목표 | 판정 |
|---|---|---|---|---|
| 지문 | 29/30 (96.7%) | 29/30 (96.7%) | ≥80% | ✅ 통과 |
| 허밍 | 10/20 (50.0%) | **14/20 (70%)** | ≥50% | ✅ 통과 |

raw 허밍 50.0%는 목표선과 정확히 같은 여유 0의 수치지만, MISS 10건을 `resultAcrid`로 전수 검증한 보정치는 70%다(보정 근거는 아래 — 판정 로직이 못 잇는 표기·커버 반환을 수동 확인한 것으로, 오답을 정답 처리한 게 아니다). 보정분은 전부 manifest acrid로 고정했으므로 **다음 런부터는 raw가 실질에 수렴**한다.

### 지문 — acrid 고정 워크플로 실증

- 07-29 파일럿에서 고정한 29곡 acrid 덕에 **표기 차이 MISS 0건** — raw 16/30이던 1차와 달리 이번엔 29곡 전부 raw HIT. 표기 혼재(한글/로마자·괄호·악센트) 문제는 acrid 고정으로 완전히 제거됨을 실증.
- 유일 MISS는 '엘리제를 위하여' — "I Gotta Feeling — Dean Millen" score 100 **재현**(1차와 동일 오답). ACR DB 오라벨 확정적. 흥미: **같은 곡 허밍 클립은 HIT**(멜로디 DB에는 정상 수록). 클립 교체 검토 대상(교체 성공 시 30/30 가능).

### 허밍 MISS 10건 분류 (`resultAcrid` 검증)

1. **표기 차이 1건 — 밤편지**: top1 "Through the Night — IU"(0.96) = 공식 영문 제목·동일 아티스트로 정답. looseMatch는 한↔영 제목을 못 잇는다 — acrid 고정.
2. **커버 반환 3건(사용자 확정)**: 보고 싶다→"I Miss You — Lim Young Woong"(0.8, 임영웅 커버) · 서른 즈음에→"around thirty — HYUK"(0.98) · 인연→"인연 (From 왕의 남자) — 함준영"(0.74). 곡 식별은 정확하므로 07-29 확립한 기준대로 HIT 취급, acrid는 해당 커버 기준으로 고정.
3. **진성 미스 6건**: 무결과 3(좋은 날 — 1차 재현·사건의 지평선·신호등) + 동명이곡 오매칭 2(거리에서×2 — 이가현 클립은 윤종신 재현, 신규 클립은 WAX 반환·동일 곡 여부 미확정이라 보수적으로 MISS 유지) + 완전 오매칭 1(소주 한 잔→핀란드 곡 0.67).

### 커버리지 가설 — 실측으로 확정

- **글로벌 3/3(100%) vs K-곡 raw 7/17(41%), 진성 미스 6건 전부 K-곡.** 07-16 오매칭 진단에서 세운 "ACR 허밍 DB의 K-곡 커버리지 부족" 가설이 20곡 전수로 확정됐다.
- K-곡은 맞아도 원곡 대신 **커버·영문 등재로 반환**되는 패턴 일관(이찬원·임영웅·HYUK·함준영·inst.) — 같은 뿌리(원곡 미수록·커버만 수록)의 증거.
- 함의: 병목은 녹음 품질이 아니라 DB 커버리지 → **녹음 품질 개선(조건부 백로그)은 착수 불필요**, AI 폴백(spec 002)·심층 탐색(spec 004)이 정확히 이 갭을 메우는 구조라는 서사가 실측으로 성립.

**남은 것**: ①엘리제 지문 클립 교체 검토 ②신규 12클립 singer 기입(미스 편중 분석용) ③분기 2·3회차 재측정(acrid 고정 상태라 raw로 바로 판정 가능).
